PRIMARY:

- instead of loading a local image file for the site image, use catmox.moe to host the celestial image and send a request to get the image
- Whitelist Management > User Management
- make dashboard have a functional "Total Users" card
- create a custom toast/notification system instead of using browser alerts??

Hashing:

- MD5
- SHA-1
- SHA-256
- SHA-512
- Identify Hash

Encoding / Decoding:

- Base64 (encode/decode)
- Base64URL (encode/decode)
- URL Encode / Decode
- Quoted-Printable Encode / Decode
- SAML Encode
- Pretty JSON
- button to copy the resultant

Binary Converison:

- Text ➝ Binary
- Binary ➝ Text

Color Conversion:

- Dropdown: Input Unit (RGB / HEX / HSV / HSL, etc.)
- Arrow ➝
- Dropdown: Output Unit (RGB / HEX / HSV / HSL, etc.)
- Add a color picker input (so users can select a color and instantly see conversions).
- Show preview boxes of the color in each representation (HEX, RGB, etc.) automatically.

(so you can do things like RGB ➝ HEX, HSV ➝ RGB, etc. all in one panel)

Generators:

- Random number generator (with min/max slider)
- Advanced string generator: let users toggle which character sets to include (lowercase, uppercase, numbers, symbols).
- random key generaor: uses the system generate key function used in whitelisting. contains a slider to define a min and max length
- Random UUID (v4)

Time Tools:

- Unix timestamp ➝ Human date/time
- Dropdown: Convert to timezone
    - Local (default)
    - Specific TZs (UTC, EST, PST, etc.)
- Add the reverse too: Date/Time ➝ Unix timestamp.

Whitelist Management:

- JSON editor box (raw GitHub whitelist file)
- Discard changes button (revert to original fetch)
- Apply changes button (push update to GitHub)
- Export whitelist ➝ JSON / CSV
- Import whitelist ➝ JSON / CSV
    - Modal with drag + drop file upload
    - Preview of what users will be added
- Search bar inside the JSON editor (so you don’t need to scroll manually). dont just search the input content and show only the search query text in the input, select the search query text inside the input if the search query was found.
- On import: show a diff view of what’s being added vs. what’s already there.






















































UTILITIES:

- hash: md5, sha1, sha256, sha512
- encode/decode:
    - Base64
    - Base64Url
    - URL Encode
    - MD5 Hash
    - SAML Encode
    - Pretty JSON
    - Quoted-Printable Encode
- binary | text > binary & binary > text
- color conversion: rgb > hex | hex > rgb | HSV > RGB | HSL > RGB | really just any combination of color units. use a dropdown and a arrow to tell the site which units to convert
    e.g. RGB ----> HEX (Hexadecimal) OR HSV ----> RGB, etc...
- random: generate a random number/string | accept a slider for min and max length | be advanced enough to act as a random number OR string generator with a customizable length range
- identifyhash: check the identity of a hashed string by checking the character length
- time: convert a Unix timestamp (1758589080) to a human readable time/date. a dropdown to convert the Unix timestamp to a specific timezone, with the first option "Local" as the default. The default "Local" will convert the given Unix timestamp to your local timezone.
- tab: Whitelist Management
    - an input that contains the raw, json text of the whitelist, fetched from the github file
    - button to discard or apply changes to the whitelist. the discard button will just revert all the changes the user has made to the input
    - export button to export the whitelist to either JSON or CSV
    - import button to bulk import users. supports importing with a JSON or CSV file. a modal for drag and drop?? when uploading a file, show what users were added